{
  "name": "ssr",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host",
    "build": "astro build",
    "build:static": "BUILD_OUTPUT=static astro build",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "18"
  },
  "main": "index.js",
  "dependencies": {
    "@astrojs/image": "^0.16.1",
    "@astrojs/node": "^5.1.0",
    "@cloudcommerce/api": "^0.7.0",
    "@cloudcommerce/firebase": "^0.7.0",
    "@cloudcommerce/ssr": "^0.7.0"
  },
  "devDependencies": {
    "@cloudcommerce/i18n": "^0.7.0",
    "@cloudcommerce/storefront": "^0.7.0",
    "@cloudcommerce/types": "^0.7.0"
  }
}
