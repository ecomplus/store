{
  "name": "ssr",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd storefront && npx astro dev",
    "build": "cd storefront && npm i && npx astro build",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "16"
  },
  "main": "index.js",
  "dependencies": {
    "@cloudcommerce/firebase": "^0.0.90",
    "@cloudcommerce/ssr": "^0.0.90"
  }
}
