---
import loadPageContext, { PageContext } from '@cloudcommerce/storefront/src/lib/ssr-context';
import FallbackMain from '@cloudcommerce/storefront/src/lib/main/Fallback.astro';
import PagesLayout from '../layouts/Pages.astro';

let pageContext: PageContext;
try {
  pageContext = await loadPageContext(Astro);
} catch (err) {
  return err.astroResponse;
}
---

<!DOCTYPE html>
<html lang={pageContext?.lang.replace('_', '-')}>
<PagesLayout pageContext={pageContext}>
  <FallbackMain pageContext={pageContext} />
</PagesLayout>
</html>
